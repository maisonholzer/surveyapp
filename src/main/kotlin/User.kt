package edu.uiowa.cs

// just to show it can be done, the User class is moved to
// a separate file (which would enable a team to work on
// different files of a project conveniently)

data class User(val username: String, val password: String, val age: Int)
